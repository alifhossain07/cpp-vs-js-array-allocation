
let arr = [];
for (let i = 1; i <= 5; i++) 
    arr.push(i); 
console.log("Heap Dynamic:", arr.join(" "));

//The array starts empty and dynamically resizes as new elements are added with push(). JavaScript arrays are always allocated on the heap