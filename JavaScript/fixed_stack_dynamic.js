//A Fixed Stack-Dynamic Array is an array with a fixed size determined at compile time, but the memory is allocated at runtime on the stack when a function is called.

// In JavaScript, Arrays are Dynamic by Default. So I created an array with 5 elemnts to represent a fixed stack-dynamic array.
const arr = [1, 2, 3, 4, 5];
console.log("Fixed Stack Dynamic:", arr.join(" "));
